/**
 * Shared semantic color palette for the dashboard's status/metric accents
 * (ledger cards, security seal, share-token flow, consents timeline).
 * One source of truth so these hexes aren't hand-copied per page.
 */
export const kvColors = {
  signal: "#3452D9",
  verify: "#1C8C74",
  danger: "#DF4949",
} as const;
